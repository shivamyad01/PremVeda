<div class="header__recently-viewed hidden block-1025">
  <a
    href="/pages/recently-viewed-products"
    class="header-icon header-color {% if type != 'header_mega' %}w-44 h-44 inline-flex content-center{% else %}flex align-center gap-10 no-underline{% endif %}"
    aria-label="Recent view product"
  >
    <svg width="22" height="22" viewBox="0 0 15 15" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
      <use href="#icon-recently-view" />
    </svg>
    {% if type == 'header_mega' %}
      {{ 'templates.rvp.rvp' | t }}
    {% endif %}
  </a>
</div>
